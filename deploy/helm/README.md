# Helm chart for Ctxward (skeleton — flesh out in M2)

This chart will land in **M2** with full templates (Deployment, Service, ConfigMap, Secret, ServiceMonitor, PrometheusRule, HPA, PDB, Ingress).

For M1, see [`deploy/systemd/ctxward.service`](../systemd/ctxward.service) for VM deploys, or `docker-compose.yml` at the repo root for local stacks.

Tracking issue: M2 milestone.
